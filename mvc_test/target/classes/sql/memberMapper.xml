<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="memberMapper">
	<insert id="insertMember" parameterType="net.koreate.vo.MemberVO">
		INSERT INTO test_members VALUES(#{userid},#{userpw},#{username},#{email},now(),now())
	</insert>
</mapper>